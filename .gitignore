# models / big binaries — never commit
*.gguf
*.bin
*.safetensors
# build + run artefacts
llama.cpp/
build/
*.log
logs/
# local machine paths / secrets
.env
*.local
config.local.sh
.DS_Store
# personal memory / KV state — never commit
memory/
*.db
*.pid
